<p id="notice"><%= notice %></p>

<h1><%= @user.username  %></h1>

<table class="table">
  <thead>
    <tr>
      <th>Phone number</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= @user.phone_number %></td>
      <td><%= @user.email %></td>
    </tr>
  </tbody>
</table>

<%= button_to 'Send Alert', sendsms_path %>

<%= link_to 'New User', new_user_path %>
